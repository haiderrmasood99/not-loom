# not-loom
Local Loom-style recorder: React/Vite frontend uses in-browser MediaRecorder to capture screen + mic + webcam overlay to WebM; Flask/PyAudio/OpenCV backend (optional) can screen-capture with webcam overlay and merge via ffmpeg; all storage is local.
